node_modules/
.env
data/
cache/
*.log

# 个人品味语料 —— 含作息等私人信息，不提交。
# 只提交 *.example.md 模板，clone 下来后 cp 一份即可。
user/*.md
!user/*.example.md

# 平台导出的原始曲库 —— 完整收听记录，比语料本身更私密
user/raw/

.DS_Store

# 第三方参考素材（他人原创内容）——只留本地，绝不提交。
# diagram/ 下你自己画的架构图不受影响，可以正常提交。
diagram/reference/
